# HubSpot (Secao 8.2). Token de Private App com scope de contatos (crm.objects.contacts.write).
# Sem esta variavel, a serverless /api/submit-lead responde 200 e pula o HubSpot (dev/preview).
HUBSPOT_PRIVATE_APP_TOKEN=
